[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PDF-Optimize"
version = "0.1.0"
dependencies = ["Flask", "pikepdf", "gunicorn", "werkzeug"]